// @import '@/myApp/styles/mixins';
@import '../../../myApp/styles/mixins';

.card {
  width: 80vw;
}

.createPostModal {
  overflow: unset;

  // !  ЕСЛИ МЕНЯТЬ ЭТО, то менять это еще и в leftContentSwiper и boxContent
  @include modal-height;
  padding: 0;
}

.boxContent {
  position: relative;

  display: flex;
  flex-flow: column nowrap;
  align-items: center;
  justify-content: center;

  box-sizing: border-box;
  // height: 54vh;
  @include modal-height;

  min-width: 50vw;
  max-width: 60vw;
  // min-height: 40vh;

  &.fullWidth {
    flex-direction: column;
  }

  &.splitContent {
    flex-direction: row;
    justify-content: space-evenly;

    width: 100%;
    max-width: unset;
    min-height: unset;
  }

  .leftContent {
    width: 50%;

    .leftContentSwiper {
      height: 54vh;
      @include modal-height;
    }
  }
}

.imageElement {
  width: 100%;
  height: 100%;
  object-fit: contain;
}

.rightContent {
  // empty
}

.addImg {
  position: absolute;
  right: 0;
  bottom: 0;
}

.filtersPanel,
.publicationPanel {
  width: 50%;
  padding: 10px;
}

.filtersPanel {
  display: flex;
  height: 100%;
  padding: 10px 55px;
}

.imageWrapper {
  display: flex;
  align-items: center;
  justify-content: center;

  width: 100%;
  height: 100%;

  &.splitContent {
    width: 50%;
  }
}

.boxErrorMessage {
  display: flex;
  justify-content: center;
  margin-bottom: 34px;
}

.errorMessage {
  width: 397px;
  padding: 6px 24px;

  color: var(--light-100);
  text-align: center;

  background-color: var(--danger-900);
  border: 1px solid var(--danger-300);
}
