.card {
  width: 100%;
  max-width: 486px;

  .text {
    margin-bottom: 24px;
  }

  .buttonsWrapper {
    display: flex;
    justify-content: space-between;

    .yes {
      color: var(--accent-500);
    }
  }
}
